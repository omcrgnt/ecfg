// Package config loads typed configuration from environment variables without a registry.
//
// Use [Parse] for standalone apps and tests. Org bootstrap via side-registry
// is [github.com/omcrgnt/ecfg.LoadEnv] in the root ecfg package.
package config
